'use strict';

/**
 * team-structure controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::team-structure.team-structure');
